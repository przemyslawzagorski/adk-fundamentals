GOOGLE_GENAI_USE_VERTEXAI=1
GOOGLE_CLOUD_PROJECT=adk-training-pz
GOOGLE_CLOUD_LOCATION=us-central1

# Model - domyslnie gemini-2.5-flash, mozna zmienic na gemini-2.5-pro dla wyzszej jakosci
ADK_MODEL=gemini-2.5-flash
ADK_MODEL_STRONG=gemini-2.5-pro

# --- Comarch MCP (Jira + Wiki + GitLab) ---
COMARCH_MCP_REGISTRY=https://nexus.czk.comarch/repository/ai-npm
NODE_EXTRA_CA_CERTS=C:\Users\TWOJ_LOGIN\Documents\cert\GK_COMARCH_ROOT_CA.crt

# Jira
JIRA_BASE_URL=https://tapir.krakow.comarch/jira/rest/api/2
JIRA_BEARER_TOKEN=<twoj-token-jira>

# Wiki (Confluence)
WIKI_BASE_URL=https://wiki.comarch/rest/api
WIKI_BEARER_TOKEN=<twoj-token-wiki>

# GitLab
GITLAB_BASE_URL=https://gitlab.czk.comarch/api/v4
GITLAB_TOKEN=<twoj-token-gitlab>

# Katalog wyjsciowy dla generowanych dokumentow
OUTPUT_DIR=./output

# Katalog ze skillami (built-in + generated)
SKILLS_DIR=./skills

# =============================================================================
# Direct REST clients (zamiast MCP) — uzywane przez orchestrator ticket_to_hld
# =============================================================================
# Jira Data Center (REST /rest/api/2) — PAT (Personal Access Token)
JIRA_DC_BASE_URL=https://tapir.krakow.comarch/jira
JIRA_DC_PAT=<twoj-PAT-jira>
JIRA_DC_VERIFY_SSL=true
JIRA_DC_TIMEOUT_S=30

# Confluence Data Center (REST /rest/api) — PAT
CONFLUENCE_DC_BASE_URL=https://wiki.comarch
CONFLUENCE_DC_PAT=<twoj-PAT-wiki>
CONFLUENCE_DC_VERIFY_SSL=true
CONFLUENCE_DC_TIMEOUT_S=30

# NotebookLM (opcjonalnie — Playwright/Computer Use)
NOTEBOOKLM_ENABLED=false
NOTEBOOKLM_NOTEBOOK_URL=

# Pipeline tuning
ANALYST_MAX_CRITIQUE_ITERATIONS=3
