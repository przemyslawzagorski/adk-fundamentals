# =============================================================================
# WYMAGANE — Vertex AI / Google AI
# =============================================================================
GOOGLE_GENAI_USE_VERTEXAI=1
GOOGLE_CLOUD_PROJECT=adk-training-pz
GOOGLE_CLOUD_LOCATION=us-central1

# =============================================================================
# WYMAGANE — Ścieżki projektu i indeksu
# =============================================================================
# Ścieżka do projektu do zaindeksowania (domyślnie ./sample_project)
CODE_PROJECT_DIR=./sample_project
# Ścieżka do persystentnego indeksu (LlamaIndex SimpleVectorStore)
CODE_INDEX_DIR=./index_store

# =============================================================================
# OPCJONALNE — GitHub MCP (publiczny, dla każdego z tokenem GitHub)
# =============================================================================
# Wygeneruj token: https://github.com/settings/tokens (scope: repo, read:org)
# Po ustawieniu agent automatycznie zyska dostęp do: repos, issues, PRs, code search
# GITHUB_PERSONAL_ACCESS_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# =============================================================================
# OPCJONALNE — Comarch MCP (wewnętrzny, wymaga VPN/sieci Comarch)
# =============================================================================
# Wymagane tylko jeśli chcesz integrację z Jira/GitLab/Wiki Comarch.
# Agent automatycznie sprawdza dostęp sieciowy — jeśli VPN wyłączony, pomija MCP.
# COMARCH_MCP_REGISTRY=https://nexus.czk.comarch/repository/ai-npm
# NODE_EXTRA_CA_CERTS=C:\Users\TWOJ_LOGIN\Documents\cert\GK_COMARCH_ROOT_CA.crt

# Jira
# JIRA_BASE_URL=https://tapir.krakow.comarch/jira/rest/api/2
# JIRA_BEARER_TOKEN=<twoj-token-jira>

# Wiki (Confluence)
# WIKI_BASE_URL=https://wiki.comarch/rest/api
# WIKI_BEARER_TOKEN=<twoj-token-wiki>

# GitLab
# GITLAB_BASE_URL=https://gitlab.czk.comarch/api/v4
# GITLAB_TOKEN=<twoj-token-gitlab>
