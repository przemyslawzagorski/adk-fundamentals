[pytest]
testpaths = tests
pythonpath = .
addopts = -ra -q -m "not live"
asyncio_mode = auto
markers =
    live: testy wymagajace prawdziwego Vertex AI (pomijane domyslnie)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
