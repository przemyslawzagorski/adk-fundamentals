{
  "model_context_policy": {
    "default": {
      "include_files": ["**/*.java", "**/*.xml", "**/*.properties"],
      "exclude_files": [
        "**/target/**",
        "**/build/**",
        "**/.git/**",
        "**/.vscode/**",
        "**/.idea/**",
        "**/node_modules/**",
        "**/dist/**",
        "**/*.log"
      ],
      "max_tokens": 8192,
      "strategy": "prioritized_recent"
    },
    "prompts": {
      "documentation_generation": {
        "include_files": ["src/main/java/**/*.java", "src/main/resources/**/*.xml"],
        "strategy": "entire_project_if_small",
        "max_tokens": 16384
      },
      "refactoring": {
        "include_files": ["*.java"],
        "strategy": "focused_file_only",
        "max_tokens": 4096
      },
      "test_generation": {
        "include_files": ["src/main/java/**/*.java", "src/test/java/**/*.java"],
        "strategy": "related_files_and_target",
        "max_tokens": 12288
      }
    }
  },
  "prompt_templates": {
    "documentation_service_layer": {
      "description": "Generuje dokumentację dla warstwy serwisowej.",
      "template": "Jako architekt oprogramowania, przygotuj szczegółową dokumentację techniczną dla warstwy serwisowej w projekcie spring-petclinic. Opisz główne serwisy (OwnerService, VetService, PetService, VisitService), ich kluczowe metody, odpowiedzialności oraz wzajemne zależności. Zwróć uwagę na przepływ danych i interakcje między nimi. Dokumentacja powinna być zwięzła, ale kompleksowa. Użyj @workspace, aby uzyskać pełen kontekst. Format: Markdown."
    },
    "refactor_stream_api": {
      "description": "Refaktoryzuje metodę do użycia Stream API.",
      "template": "Zrefaktoryzuj metodę `findVets()` w tym pliku. Chcę, aby zwracała weterynarzy posortowanych alfabetycznie po nazwisku (lastname) przed zwróceniem kolekcji. Użyj Java Stream API do sortowania. Ogranicz kontekst tylko do tego pliku. #file"
    },
    "generate_owner_integration_tests": {
      "description": "Generuje testy integracyjne dla OwnerController.",
      "template": "Jako doświadczony inżynier testów, napisz kompleksową klasę testów integracyjnych dla `OwnerController.java` w projekcie `spring-petclinic`. Klasa powinna nazywać się `OwnerControllerIntegrationTests` i używać `Spring MockMvc` oraz `JUnit 5`. Obejmij testy dla pomyślnego dodania właściciela, dodania z błędami walidacji, wyszukiwania nieistniejącego nazwiska, wyszukiwania jednego wyniku i wyszukiwania wielu wyników. Użyj `@MockBean` dla `OwnerRepository` i zasymuluj jego zachowanie za pomocą `BDDMockito.given()` w metodzie `@BeforeEach`. Zwróć tylko kod źródłowy klasy testowej w Javie."
    },
    "validate_pet_age": {
      "description": "Implementuje walidację wieku zwierzęcia w Pet.java i PetController.java.",
      "template": "Jako doświadczony programista Spring Boot, zaktualizuj klasę `Pet.java` i metodę `processUpdateForm` w `PetController.java`. W `Pet.java`, upewnij się, że pole `age` jest wymagane (nie null) i jest większe od zera, używając adnotacji walidacyjnych `@NotNull` i `@Min(1)`. Następnie, w `PetController.java`, upewnij się, że metoda `processUpdateForm` poprawnie obsługuje błędy walidacji z `BindingResult`. W przypadku błędów walidacji, formularz `pets/createOrUpdatePetForm` powinien zostać ponownie renderowany, przekazując `pet` i `owner` do modelu. Zapewnij, że `BindingResult` jest prawidłowo przetwarzany, aby komunikaty walidacyjne z adnotacji były wyświetlane. Ogranicz kontekst do `PetController.java` i `Pet.java`. #file\nZwróć zaktualizowany kod dla obu plików."
    }
  }
}